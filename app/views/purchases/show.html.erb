<div class="category_show_title  m-0 p-0 text-dark">
  <h1><strong>Thank you for your purchase!</strong></h1>
  <p>Here is a summary of the design you just purchase</p>
</div>
<div class="container">
  <div class="design_card">
    <div class="design_img">
      <%= cl_image_tag @design.photo.key %>
    </div>
    <div class="informations">
      <div class="design_title">
        <h1><%= @design.name %></h1>
        <h2>$ <%= @design.price %></h2>
      </div>
      <div class="design_info">
        <div class="compart">
          <p>Designer : <%= @design.user.first_name %> <%= @design.user.last_name %></p>
          <p>Layers : <%= @design.layers %></p>
        </div>
        <div class="compart">
          <p>File resolution: 300 dpi</p>
          <p>Fyle type : <%= @design.file_type %></p>
        </div>
        <div class="compart">
          <p>Categories : <% @design.categories.each do |cat| %>
              <%= cat.name  %>
            <% end %>
          </p>
          <p>
            Keywords : <% @design.tags.each do |tag| %>
              <%= tag.name  %>
            <% end %>
          </p>
          <p>Created at: <%= @design.created_at.strftime("%Y-%m-%d") %></p>
        </div>
      </div>
    </div>
  </div>
  <div class="d-flex justify-content-between">
    <%= link_to 'Back to categories', categories_path, class: 'button' %>
    <%= link_to 'My purchases', categories_path, class: 'button' %>
  </div>
</div>
